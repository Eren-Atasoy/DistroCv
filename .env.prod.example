# =============================================================================
# PRODUCTION ENVIRONMENT — KOYEB
# =============================================================================
# Bu dosyayı .env.prod.example'den kopyala:
#   cp .env.prod.example .env.prod
# UYARI: Bu dosyayı asla git'e commit etme!
# =============================================================================

# --- Koyeb ---
KOYEB_API_TOKEN=your-koyeb-api-token
KOYEB_APP_NAME=distrocv
KOYEB_API_SERVICE_NAME=api
KOYEB_FRONTEND_SERVICE_NAME=frontend

# --- Database (Koyeb Managed Postgres / Neon) ---
# Koyeb dashboard'dan "Database" oluşturunca bu URL otomatik verilir
DATABASE_URL=postgresql://user:password@ep-xxxxx.eu-west-2.aws.neon.tech/distrocv?sslmode=require

# --- Redis (Upstash) ---
# https://console.upstash.com adresinden Redis oluştur
UPSTASH_REDIS_URL=rediss://default:xxxxx@global-xxxxx.upstash.io:6380

# --- Backend ASP.NET Core ---
ASPNETCORE_ENVIRONMENT=Production
CONNECTION_STRING=Host=ep-xxxxx.eu-west-2.aws.neon.tech;Port=5432;Database=distrocv;Username=user;Password=password;SSL Mode=Require;Trust Server Certificate=true

# --- AWS S3 (file storage) ---
AWS_REGION=eu-west-1
AWS_S3_BUCKET=distrocv-prod-files
# Koyeb'de AWS credential'ları env olarak set edilir:
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key

# --- JWT Auth ---
JWT_SECRET=production-icin-cok-guvenli-bir-secret-key-olustur!!!
JWT_ISSUER=distrocv-api
JWT_AUDIENCE=distrocv-client

# --- Google OAuth ---
GOOGLE_CLIENT_ID=your-prod-google-client-id

# --- Gemini AI ---
GEMINI_API_KEY=your-prod-gemini-api-key

# --- Frontend (Koyeb'deki API URL) ---
# Koyeb deploy sonrası API servisinin URL'si
VITE_API_URL=https://distrocv-api-USERNAME.koyeb.app/api
